package com.ucheuxing.push.server.util;


/**
 * 解析数据util
 * 
 * @author fei
 * 
 */
public class ParseDataUtil {
	/**
	 * 解析文件bean数据
	 * 
	 * @param in
	 * @param fileInfo
	 */
	/*
	 * public static void parseDataToFileInfo(BinaryInputStream in, FileInfo
	 * fileInfo) { fileInfo.fileType = in.readInt(); fileInfo.fileName =
	 * in.readString(); fileInfo.size = in.readLong(); fileInfo.clientId =
	 * in.readString(); fileInfo.createTime = in.readLong(); fileInfo.uploadTime
	 * = in.readLong(); fileInfo.fileUrl = in.readString();
	 * fileInfo.thumbnailUrl = in.readString(); fileInfo.duration =
	 * in.readLong(); fileInfo.album = in.readString(); fileInfo.artist =
	 * in.readString(); fileInfo.filePath = in.readString(); Log.d("liyi",
	 * "fileInfo: " + fileInfo.toString()); }
	 *//**
	 * 解析文件bean数据
	 * 
	 * @param in
	 * @param fileInfo
	 */
	/*
	 * public static void parseDataToGroupInfo(BinaryInputStream in, GroupInfo
	 * groupInfo) { groupInfo.groupId = in.readString(); groupInfo.groupName =
	 * in.readString(); groupInfo.fileNum = in.readInt(); }
	 */
}
